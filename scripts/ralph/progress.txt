# Fazumi Ralph Loop — Progress Tracker
# Format: STORY_ID = STATUS [DONE | IN_PROGRESS | PENDING | BLOCKED]
# Update this file after each story completes.

S001 = PENDING  # Next.js scaffold + dev tooling
S002 = PENDING  # Supabase DB schema + RLS
S003 = PENDING  # Paste text → AI summary → display
S004 = PENDING  # File upload (txt + zip)
S005 = PENDING  # Google + Apple OAuth
S006 = PENDING  # Persist summaries + usage tracking
S007 = PENDING  # Dashboard: history list + detail view
S008 = PENDING  # Lemon Squeezy payments + webhook
S009 = PENDING  # Plan limits enforcement
S010 = PENDING  # Referral system

# 2026-02-28
# Hydration mismatch fix completed for theme/lang client preferences and browser-injected attributes.
# Files: app/layout.tsx, public/hydration-sanitize.js, lib/context/ThemeContext.tsx, lib/context/LangContext.tsx, lib/hooks/useMounted.ts, components/landing/Nav.tsx, components/layout/TopBar.tsx, tasks/lessons.md
# Pattern: use useSyncExternalStore for browser preference state that must match SSR on the first hydration pass.

# 2026-02-28
# Product loop bugfixes: summarize persistence is explicit, daily limits use one UTC key, dashboard widgets/notifications now derive from saved summaries, and local dev helpers can seed test accounts/set plans.
# Files: app/api/summarize/route.ts, lib/limits.ts, lib/dashboard-insights.ts, lib/hooks/useDashboardInsights.ts, components/widgets/CalendarWidget.tsx, components/widgets/TodoWidget.tsx, components/layout/TopBar.tsx, components/dashboard/DashboardBanner.tsx, app/summarize/page.tsx, app/api/dev/create-test-accounts/route.ts, app/api/dev/set-plan/route.ts, README.md, tasks/lessons.md
# Pattern: for any product loop that depends on persistence, drive UI from saved rows rather than sample data and fail loudly when the write path breaks.

# 2026-02-28
# Lemon Squeezy env naming is standardized around canonical names with a legacy webhook-secret fallback so local replay and production config resolve the same values.
# Files: app/api/webhooks/lemonsqueezy/route.ts, scripts/webhooks/replay-lemonsqueezy.js, scripts/webhooks/README.md, README.md, .env.local.example
# Pattern: when env names drift, keep one canonical name in docs and accept a narrow legacy alias only at the boundary that reads the value.

# 2026-03-01
# Reliability bolt-ons added: Sentry error monitoring for App Router, structured JSON server logs for summarize/webhook routes, and a tighter health response plus deploy smoke checklist.
# Files: app/api/summarize/route.ts, app/api/webhooks/lemonsqueezy/route.ts, app/api/health/route.ts, app/global-error.tsx, instrumentation.ts, instrumentation-client.ts, sentry.server.config.ts, sentry.edge.config.ts, lib/logger.ts, lib/sentry.ts, next.config.ts, README.md, .env.local.example
# Pattern: for pre-production observability, keep capture payloads intentionally sparse by hashing identifiers, redacting content-like keys, and logging route-safe metadata only.

# 2026-03-02
# OAuth flow hardened: login now preserves `next` through Google/Apple and email callback URLs, uses an explicit browser redirect from the returned provider URL, and the auth callback normalizes the post-login destination across proxied hosts.
# Files: app/login/page.tsx, app/auth/callback/route.ts, tasks/lessons.md
# Pattern: when an auth flow crosses origins, always carry a sanitized relative return path through the callback and do not rely on implicit provider redirects alone.

# 2026-03-02
# Dev-only admin dashboard added with overview, users, income, guarded admin APIs, and polling-based recent subscription notifications backed by the Supabase service role.
# Files: app/admin_dashboard/**, app/api/admin/**, components/admin/**, lib/admin/**, lib/supabase/admin.ts, lib/supabase/middleware.ts, .env.local.example, docs/decisions.md
# Pattern: keep internal operator tooling behind a development-only cookie gate and centralize all service-role access in server-side helpers so pages and APIs share one audited path.

# 2026-03-03
# Landing conversion slice completed: larger Apple-style typography, rotating hero headline, interactive gated demo, and How It Works screenshot placeholders with updated copy.
# Files: app/globals.css, components/landing/Hero.tsx, components/landing/HowItWorks.tsx, components/landing/Pricing.tsx, components/landing/FAQAccordion.tsx, components/ui/accordion.tsx, specs/hero-redesign-typography-bv4-bv6.md, tasks/todo.md
# Pattern: keep landing demos local and deterministic so users can preview the product without adding backend/storage complexity or risking raw chat persistence.

# 2026-03-03
# Typography overhaul landed: Inter + Cairo font loading, corrected mobile/desktop scale tokens, Arabic line-height refinements, and consistent 16px input/form text across landing, dashboard, history, and auth/contact surfaces.
# Files: app/globals.css, app/layout.tsx, app/dashboard/page.tsx, app/login/page.tsx, app/summarize/page.tsx, components/contact/ContactForm.tsx, components/dashboard/DashboardBanner.tsx, components/history/HistoryList.tsx, components/landing/Hero.tsx, components/landing/HowItWorks.tsx, components/landing/Pricing.tsx, components/landing/FAQAccordion.tsx, components/landing/FAQ.tsx, components/landing/GoToAppButton.tsx, components/landing/Newsletter.tsx, components/landing/Testimonials.tsx, components/ui/accordion.tsx, components/ui/input.tsx, components/ui/textarea.tsx, specs/typography-overhaul-2026-saas-standards.md, tasks/todo.md
# Pattern: when typography drifts across a bilingual product, fix the root tokens and shared primitives first, then apply explicit component-level sizes only where the hierarchy must differ from the global defaults.

# 2026-03-03
# Launch blocker fix completed: the global App Router error boundary is now hook-free, root themeColor moved to viewport, and production verification is green without the prior metadata warnings.
# Files: app/global-error.tsx, app/layout.tsx, specs/launch-blockers-p0-fix.md, tasks/todo.md, tasks/lessons.md
# Pattern: for special Next.js boundary files, prefer self-contained markup first, then verify the live build output before applying broader cleanup work.

# 2026-03-03
# Follow-up build blocker verification completed: clearing `.next` and `node_modules/.cache` plus `pnpm store prune` removed the stale-failure state, and the clean build/lint/typecheck pass without extra app-code changes.
# Files: specs/build-errors-p0-fix-2.md, specs/launch-blockers-p0-fix.md, tasks/todo.md, scripts/ralph/progress.txt
# Pattern: when a Next.js production failure disappears after a clean cache reset, record the clean-run evidence before rewriting boundary components or route pages.

# 2026-03-03
# Public-route prerender hardening completed: added a custom App Router 404 page and defaulted localized section item arrays to [] before rendering list lengths/maps on legal/help pages.
# Files: app/not-found.tsx, app/cookie-policy/page.tsx, app/privacy/page.tsx, app/terms/page.tsx, app/help/page.tsx, tasks/todo.md, scripts/ralph/progress.txt, tasks/lessons.md
# Build status: `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass; build reached `Generating static pages using 7 workers (36/36)`. `pnpm test` is currently blocked by Playwright webServer startup timing out after port 3000 was already occupied.
# Pattern: for localized collections rendered in SSR paths, coerce the selected value to a concrete array before any `.length` or `.map` access instead of relying on the localization helper to always return one.

# 2026-03-03
# Final launch-blocker pass completed: cleared `.next` and `node_modules/.cache`, pruned the pnpm store, replaced `app/error.tsx` with inline SSR-safe route boundary UI, and re-audited the legal/help pages for concrete array normalization.
# Files: app/error.tsx, specs/launch-blockers-final-fix.md, tasks/todo.md, scripts/ralph/progress.txt, tasks/lessons.md
# Build status: `pnpm lint`, `pnpm typecheck`, and a clean `pnpm build` pass; build reached `Generating static pages using 7 workers (36/36)` with exit code `0`. `pnpm test` first failed because a stale Next.js server held port `3000`, then timed out on retry after the process was stopped.
# Pattern: treat `app/error.tsx` as a special App Router boundary and keep it self-contained so route fallback rendering never depends on provider context during prerender.

# 2026-03-03
# Next.js version rollback verification completed: pinned `next` and `eslint-config-next` to `16.0.0`, refreshed the lockfile, and recorded the downgrade run in a dedicated spec/checklist.
# Files: package.json, pnpm-lock.yaml, specs/nextjs-version-fix.md, tasks/todo.md, scripts/ralph/progress.txt, tasks/lessons.md
# Build status: two clean `pnpm build` runs pass on Next.js `16.0.0` and reach `Generating static pages (36/36)` with exit code `0`; `pnpm lint` and `pnpm typecheck` pass. `pnpm test` is still blocked by `e2e/app-smoke.spec.ts` timing out while waiting for `summary-use-sample`.
# Pattern: when a production build failure points at framework internals, pin the framework version first and verify with clean caches before adding app-side suppression or route-level workarounds.

# 2026-03-07
# Audit blocker pass in progress: admin auth now fails closed without explicit credentials, shared billing entitlements derive from subscription state first, Arabic-first public route coverage is explicit, and /status no longer claims live operational health.
# Files: lib/admin/auth.ts, app/api/admin/login/route.ts, app/api/webhooks/lemonsqueezy/route.ts, lib/limits.ts, app/api/summarize/route.ts, app/api/summarize-zip/route.ts, app/billing/page.tsx, app/history/[id]/page.tsx, app/summarize/page.tsx, app/page.tsx, components/landing/GoToAppButton.tsx, app/status/page.tsx, e2e/admin-dashboard.spec.ts, e2e/public-routes.spec.ts, docs/decisions.md, docs/runbooks/payments.md, tasks/todo.md
# Pattern: when access control depends on mirrored billing/profile fields, resolve entitlements from one shared subscription-aware helper and keep every UI mirror fail-safe instead of letting stale rows grant access.

# 2026-03-07
# Verification update: `pnpm lint`, `pnpm typecheck`, and `pnpm build` now pass after the blocker patch set. `pnpm exec playwright test e2e/public-routes.spec.ts` also passes for the locale/status smoke path. The full `pnpm test` suite is still blocked locally by Playwright instability (`port 3100 already used` on one run, then worker `spawn EPERM` / timeout on follow-up runs).
# Files: tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt
# Pattern: when the full browser suite is unstable locally, record the exact failing runner condition separately from the code fixes so product risk and environment risk do not get conflated.

# 2026-03-07
# SEO hardening pass in progress: production canonicals/hreflang metadata, route-level metadata layouts, structured data expansion, sitemap/robots updates, and crawlable SSR output for `/about`.
# Files: app/layout.tsx, app/page.tsx, app/pricing/page.tsx, app/sitemap.ts, app/robots.ts, app/about/page.tsx, app/pricing/layout.tsx, app/about/layout.tsx, app/faq/layout.tsx, app/contact/layout.tsx, app/terms/layout.tsx, app/privacy/layout.tsx, app/help/layout.tsx, app/cookie-policy/layout.tsx, app/refunds/layout.tsx, app/status/layout.tsx, tasks/todo.md, scripts/ralph/progress.txt
# Pattern: when locale is cookie-based without URL prefixes, keep canonicals stable on the public path and expose language alternates from metadata rather than duplicating routes.

# 2026-03-07
# SEO hardening Round 2 completed: fixed homepage schema drift, added Twitter handle metadata and keyword coverage, enriched pricing product schema, made `/about` SSR cookie-aware for locale/RTL, and prioritized the first How It Works illustration for LCP.
# Files: app/page.tsx, app/layout.tsx, app/pricing/page.tsx, app/about/page.tsx, components/landing/HowItWorks.tsx, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. `pnpm test` was attempted twice in local CI mode and timed out instead of producing a reliable pass/fail result.
# Pattern: when SEO hardening depends on locale-specific rendering, use the same server cookie source as the root layout so SSR language, `dir`, and schema metadata do not drift from the hydrated app state.

# 2026-03-07
# SEO hardening Round 3 completed: restored the confirmed homepage social profiles and GCC geography signals, aligned the PWA manifest with the Arabic-first summarize flow, added FAQ/About JSON-LD at the route boundary, and stopped globally preloading Alexandria.
# Files: app/page.tsx, public/manifest.json, app/faq/page.tsx, app/about/page.tsx, app/faq/layout.tsx, app/layout.tsx, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a route already depends on client-only locale state, emit static JSON-LD with inline `<script type="application/ld+json">` blocks at the page/layout boundary instead of rewriting the route just for SEO metadata.

# 2026-03-07
# SEO hardening Round 4 completed: added noindex route layouts for logged-in surfaces, kept the admin dashboard layout behavior intact while blocking indexing, strengthened help/status internal linking, and upgraded `/help` plus `/status` layouts to `WebPage` JSON-LD wrappers.
# Files: app/login/layout.tsx, app/dashboard/layout.tsx, app/summarize/layout.tsx, app/history/layout.tsx, app/billing/layout.tsx, app/settings/layout.tsx, app/profile/layout.tsx, app/calendar/layout.tsx, app/admin_dashboard/(dashboard)/layout.tsx, app/help/page.tsx, app/status/page.tsx, app/help/layout.tsx, app/status/layout.tsx, specs/seo-round-4-noindex-internal-links.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a route should remain functional but stay out of search results, add route-level metadata in a thin App Router layout instead of threading robots flags through unrelated page logic.

# 2026-03-08
# SEO hardening Round 6 completed: the remaining public layouts now share hreflang alternates, and contact/help/status/legal layouts expose consistent `WebPage` plus `BreadcrumbList` JSON-LD wrappers with stable route canonicals.
# Files: app/about/layout.tsx, app/contact/layout.tsx, app/faq/layout.tsx, app/help/layout.tsx, app/status/layout.tsx, app/pricing/layout.tsx, app/terms/layout.tsx, app/privacy/layout.tsx, app/cookie-policy/layout.tsx, app/refunds/layout.tsx, specs/seo-round-6-public-layout-breadcrumbs.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when locale uses one canonical public path, expose language alternates at the metadata layer and reuse breadcrumb schema across static public layouts instead of inventing localized route variants.

# 2026-03-08
# SEO hardening Round 7 completed: the remaining public layouts now use keyword-rich absolute title tags, and a public `llms.txt` advertises Fazumi's scope, pricing, key pages, and privacy stance for AI retrieval.
# Files: app/about/layout.tsx, app/faq/layout.tsx, app/contact/layout.tsx, app/pricing/layout.tsx, app/help/layout.tsx, app/status/layout.tsx, public/llms.txt, specs/seo-round-7-absolute-titles-llms.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a route-specific title must ignore the shared root template, set `title.absolute` in the route layout and keep every other metadata field unchanged so canonicals/schema stay stable.

# 2026-03-08
# SEO hardening Round 8 completed: the remaining legal layouts now use explicit absolute titles, and the key public layouts expose route-level Open Graph metadata with relative URLs resolved by the root metadata base.
# Files: app/terms/layout.tsx, app/privacy/layout.tsx, app/cookie-policy/layout.tsx, app/refunds/layout.tsx, app/about/layout.tsx, app/faq/layout.tsx, app/pricing/layout.tsx, app/contact/layout.tsx, app/help/layout.tsx, app/status/layout.tsx, specs/seo-round-8-legal-titles-open-graph.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: for static App Router layouts that already have stable canonicals, add route-level Open Graph metadata with the same path and keep `url` relative so `metadataBase` stays the single source of truth for the absolute domain.

# 2026-03-08
# Founder supporter UX phase 0 completed in-app: founder users now get a dedicated shell identity, billing thank-you state, corrected Arabic afternoon greeting window, and profile-name sync into Auth metadata so the mounted top bar refreshes without logout/login.
# Files: specs/founder-supporter-ux-phase-0.md, tasks/todo.md, components/layout/TopBar.tsx, components/dashboard/DashboardBanner.tsx, app/billing/page.tsx, lib/i18n.ts, app/api/profile/route.ts, components/settings/SettingsPanel.tsx, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. `pnpm test` still fails on existing Playwright cases outside this slice: `e2e/admin-dashboard.spec.ts`, `e2e/app-smoke.spec.ts`, `e2e/summarize-auth.spec.ts`, and `e2e/summarize-zip.spec.ts`. The founder-related billing smoke inside `e2e/app-smoke.spec.ts` ran before the failure set.
# Pattern: when a premium tier needs distinct recognition without new routes, keep entitlement logic unchanged and layer the identity through plan-specific copy, badges, and billing-state acknowledgements.

# 2026-03-08
# UX bug batch applied: `/summarize` now lands directly on the working card with a compact inline title and privacy note under the submit row, the summarize shell is wider on laptop widths, dashboard-shell routes are grouped under `app/(dashboard)` with a scoped fade-in template, and the dashboard/landing locale toggles now show `EN / عربي` while the top-bar action icons stay visible on small phones.
# Files: specs/ux-bugs-batch-2026-03-08.md, tasks/todo.md, app/(dashboard)/template.tsx, app/(dashboard)/dashboard/**, app/(dashboard)/summarize/**, app/(dashboard)/history/**, app/(dashboard)/billing/**, app/(dashboard)/settings/**, app/(dashboard)/profile/**, app/(dashboard)/calendar/**, app/(dashboard)/todo/**, components/layout/TopBar.tsx, components/landing/Nav.tsx, components/history/types.ts, components/history/HistoryList.tsx, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. A real-browser smoke attempt was started, but local verification is still blocked here by Playwright browser `spawn EPERM` and follow-up `/summarize` navigation timeouts against the local dev server.
# Pattern: when a route-transition fix depends on App Router remount behavior, use a route group plus `template.tsx`, and keep shared types out of `app/**` so file moves do not break unrelated components.

# 2026-03-08
# Loading cleanup and founder story slice completed: the dashboard, billing, and summarize loading states are now plain first-card skeletons without mascot copy, summarize loading matches the live `max-w-4xl` width, and founders now have a noindex `/founder` dashboard route with the full story plus a billing-page link into it.
# Files: specs/loading-skeleton-founder-page-2026-03-08.md, tasks/todo.md, app/(dashboard)/dashboard/loading.tsx, app/(dashboard)/billing/loading.tsx, app/(dashboard)/summarize/loading.tsx, app/(dashboard)/founder/layout.tsx, app/(dashboard)/founder/page.tsx, app/(dashboard)/billing/page.tsx, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass after the loading cleanup and again after the founder page. Static grep verification confirms the three target loading files no longer reference `MascotArt` or `LocalizedText`. Browser/manual smoke is still pending in this environment.
# Pattern: treat dashboard loading files as structural placeholders for the live route, and keep founder storytelling in a dedicated noindex dashboard page linked from the founder billing state instead of expanding the billing card indefinitely.

# 2026-03-08
# Founder supporter UX phase 2 completed: Manrope now loads only the explicit shipped weights, Arabic shared text has roomier body and micro-text line-height, mobile tap flashes are suppressed, founders get a persistent `/founder` sidebar entry, and the dashboard shows a one-time founder welcome modal stored in browser localStorage.
# Files: specs/founder-supporter-ux-phase-2.md, tasks/todo.md, app/layout.tsx, app/globals.css, lib/i18n.ts, components/layout/Sidebar.tsx, components/founder/FounderWelcomeModal.tsx, app/(dashboard)/dashboard/page.tsx, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass after the CSS section and again after the founder UI section. `pnpm build` passes. `pnpm test` still fails on pre-existing Playwright cases outside this slice: `e2e/admin-dashboard.spec.ts`, `e2e/app-smoke.spec.ts` summarize-save smoke, `e2e/summarize-auth.spec.ts`, and `e2e/summarize-zip.spec.ts`. Real-browser/manual founder smoke is still pending in this environment.
# Pattern: keep founder-specific acknowledgement inside the existing shell, and use a browser-local "seen" flag for one-time celebratory UI that does not need to sync across devices or touch the database.

# 2026-03-08
# Playwright alignment slice completed: the transparent Fazumi logo asset is ready for its own commit, the four pre-existing Playwright failures are aligned to current admin, locale, summarize, and ZIP behavior, and repo verification is green again.
# Files: specs/playwright-test-alignment-2026-03-08.md, tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt, e2e/admin-dashboard.spec.ts, e2e/app-smoke.spec.ts, e2e/summarize-auth.spec.ts, e2e/summarize-zip.spec.ts, public/brand/logo/Fazumi logo transparent.png
# Verification: each target spec passes individually, then `pnpm lint`, `pnpm typecheck`, and `pnpm test` all pass.
# Pattern: keep browser smoke tests aligned to current UI contracts with stable selectors and explicit seeded state when the test intent is not backend schema coverage.

## Sprint: Founder UX + Pre-Launch Polish (2026-03-08)

### What changed
- Entitlement wiring: Sidebar, DashboardBanner, SettingsPanel all use resolveEntitlement()
- FounderWelcomeModal mounted on dashboard, summarize, and history routes
- Duplicate modal mount in history/page.tsx removed
- Profile page: settings link + mailto deletion link (GDPR path)
- FAQ: Arabic-support and privacy answers corrected to match implementation
- About page: unverified metrics flagged with TODO comments
- Summarize page: loading/error/limit copy aligned to voice.md
- robots.ts: /founder, /admin, /api, /calendar, /offline all blocked
- tasks/todo.md: T6 test note resolved, BV4-6, HR5-7, RC8 checked

### Files touched
components/layout/Sidebar.tsx, components/settings/SettingsPanel.tsx,
components/founder/FounderWelcomeModal.tsx, app/(dashboard)/dashboard/page.tsx,
app/(dashboard)/history/page.tsx, app/(dashboard)/summarize/page.tsx,
app/(dashboard)/profile/page.tsx, app/(dashboard)/billing/page.tsx,
app/(dashboard)/founder/page.tsx, components/landing/FAQAccordion.tsx,
app/about/page.tsx, app/robots.ts, e2e/public-routes.spec.ts, tasks/todo.md

### Patterns confirmed
- resolveEntitlement() is the single source of truth for billingPlan everywhere
- Founder badge / nav item / modal are all entitlement-driven, not profile.plan-driven
- FounderWelcomeModal localStorage guard prevents double-show across routes
