# Fazumi Ralph Loop — Progress Tracker
# Format: STORY_ID = STATUS [DONE | IN_PROGRESS | PENDING | BLOCKED]
# Update this file after each story completes.

# 2026-03-12
# Public auth-CTA hardening follow-up completed: the final audit found `PublicPageShell` routes still accepted arbitrary `sb-*-auth-token` names, so auth-cookie detection now matches only the configured Supabase project ref, the public `Go to app` fallback still redirects unauthenticated sessions to `/login?next=%2Fdashboard`, and Playwright coverage now locks both the landing and `PublicPageShell` stale-cookie regressions plus an authenticated `/pricing` nav check.
# Files: lib/supabase/auth-cookies.ts, components/landing/Nav.tsx, app/api/summarize/route.ts, lib/server/summaries.ts, e2e/support.ts, e2e/public-routes.spec.ts, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm test -- e2e/public-routes.spec.ts --grep "pricing nav keeps the app CTA for authenticated users" --project=chromium --reporter=line` passed. `pnpm test -- e2e/public-routes.spec.ts --grep "landing nav does not treat a stale auth cookie name|public shell nav does not treat a stale auth cookie name|public go-to-app CTA redirects unauthenticated cookie fallbacks to login|pricing nav keeps the app CTA for authenticated users" --project=chromium --reporter=line` passed with `3 passed, 1 skipped` before the auth-only rerun confirmed the explicit server-auth-true path. Final `pnpm lint`, `pnpm typecheck`, and `pnpm build` all passed.
# Pattern: public auth heuristics must key off the exact configured Supabase project ref, not any cookie with a similar `sb-...-auth-token` shape, and final release audits should probe both server-provided auth state and the undefined-prop fallback routes before shipping.
#
#
# 2026-03-12
# Repository health audit follow-up completed: the release-gate flake moved from test-only setup into runtime noise on public pages, so the landing nav now derives logged-in state from existing auth cookie/server signals instead of a mount-time `supabase.auth.getUser()` call, PWA/service-worker bootstrap is explicitly disabled when `PLAYWRIGHT_TEST=1`, and a `pnpm` override pins `serialize-javascript@^7.0.3` to clear the remaining Sentry/Webpack transitive vulnerability without changing framework versions.
# Files: app/layout.tsx, components/landing/Nav.tsx, components/pwa/ServiceWorkerRegistrar.tsx, package.json, pnpm-lock.yaml, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm install`, `pnpm lint`, `pnpm typecheck`, `PLAYWRIGHT_TEST=1 pnpm test`, `pnpm build`, `pnpm audit --prod`, and `pnpm exec playwright test e2e/public-routes.spec.ts --reporter=line --repeat-each 3` all pass. Main release gate result: `36 passed`, `3 skipped`, `0 failed`.
# Pattern: if a release-gate flake only appears in production-mode browser runs, audit public-page client bootstraps and service-worker behavior before tightening assertions; public chrome should avoid remote auth fetches when a cookie signal is already available, and security-only transitive CVEs are often safest to fix with a narrow package-manager override.
#

# 2026-03-12
# Public-route Playwright hardening completed: the `/founder` unauth redirect flake was traced to an unsafe locale `addInitScript` in `e2e/public-routes.spec.ts`, that helper now guards transient pre-document access, and a shared horizontal-overflow reporter plus `e2e/mobile-overflow.spec.ts` now smoke key public/authenticated routes at `375px` and `320px` without changing product UI.
# Files: e2e/public-routes.spec.ts, e2e/support.ts, e2e/mobile-overflow.spec.ts, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass. `pnpm test` passes (`36 passed`, `3 skipped`) against a fresh `next start` server on `http://127.0.0.1:3018` with `PLAYWRIGHT_TEST=1`.
# Pattern: when a browser suite mixes redirect assertions with locale/bootstrap helpers, treat init-script DOM access as timing-sensitive harness code and validate the release gate only against a freshly started build runtime after `next build`.
#

# 2026-03-11
# Supabase RLS hardening for the legacy phone-burst tables completed: added an idempotent migration that enables RLS and deny-all policies on `public.worker_phone_locks` and `public.phone_bursts`, keeping the archived WA-bot coordination flow server-only through existing helper functions.
# Files: specs/supabase-rls-hardening-phone-burst-tables-2026-03-11.md, supabase/migrations/2026031101_harden_phone_burst_tables_rls.sql, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`32 passed`, `3 skipped`). `pnpm test` required an outside-sandbox rerun because the sandbox could not resolve the remote Supabase host.
# Pattern: if a legacy internal table must stay in the exposed `public` schema for compatibility, enable RLS and fail closed instead of assuming "not used by the UI" is enough protection.
#

# 2026-03-11
# Mobile/tablet floating bottom navigation shipped: the dashboard shell now keeps the bottom dock visible through tablet widths, delays the desktop sidebar until `xl`, and adds safe-area-aware bottom clearance so lower-page actions stay reachable on `/dashboard`, `/summarize`, `/history`, `/billing`, and `/settings`.
# Files: specs/mobile-floating-bottom-nav-2026-03-11.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, components/layout/DashboardShell.tsx, components/layout/BottomNav.tsx
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: app-shell navigation breakpoints should follow the touch/navigation model instead of the first generic tablet breakpoint. If the bottom dock is the primary mobile affordance, keep it through tablet widths and reserve dock clearance in the shared shell.
#

# 2026-03-11
# Typography readability pass shipped: FAZUMI now uses `Inter` for English UI, `Cairo` for Arabic UI, a 17px reading-first body/input baseline, and a 13px floor for parent-facing helper text. The cleanup also removed lingering 9-11px copy from the landing/login/summarize/history/result/shell surfaces so the product reads more like a messaging app on phones and tablets.
# Files: specs/typography-readability-busy-parents-2026-03-11.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, app/layout.tsx, app/globals.css, components/landing/Nav.tsx, app/login/page.tsx, app/(dashboard)/summarize/page.tsx, components/SummaryDisplay.tsx, components/history/HistoryList.tsx, components/dashboard/DashboardBanner.tsx, app/reset-password/page.tsx, components/summary/FollowUpPanel.tsx, components/layout/TopBar.tsx, components/layout/Sidebar.tsx, components/layout/BottomNav.tsx, components/layout/SearchDialog.tsx, components/errors/ErrorFallback.tsx
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: if a parent-facing typography pass changes only font tokens but leaves explicit 9-11px utilities in place, the live UX will still feel cramped. Audit both the global scale and one-off size overrides together.
#

# 2026-03-11
# Summarize smoke hydration stabilized: the Playwright summarize flows now open `/summarize` through one shared readiness helper that waits for a single composer node and React hydration before interacting, which removed the last transient `summary-input` strict-mode failure from the release gate.
# Files: specs/summarize-smoke-hydration-2026-03-11.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, e2e/support.ts, e2e/app-smoke.spec.ts
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: after responsive UI changes, stabilize smoke tests around the settled hydrated control state instead of asserting on the first matching node immediately after route navigation.
# 

# 2026-03-11
# Summarize adaptive quick options added: smaller screens now get a compact accordion directly under the paste box so parents can set summary language and chat source before submitting, while the desktop-only output-language card stays in the support rail and the lower setup card focuses on saved-group organization below `xl`.
# Files: app/(dashboard)/summarize/page.tsx, specs/summarize-mobile-quick-options-2026-03-11.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass.
# Pattern: when a desktop side rail collapses on smaller screens, re-surface the essential knobs inside the primary composer with progressive disclosure instead of letting the rail become a second long interruption.
#

# 2026-03-11
# Playwright localhost IPv4 harness fix completed: local Playwright now defaults to `http://127.0.0.1:<port>` instead of `localhost`, which avoids the Windows `::1` resolution path that was causing repeated `ECONNREFUSED ::1:3000` failures during repo verification.
# Files: lib/testing/playwright.ts, tasks/lessons.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: when local browser verification is failing on Windows loopback resolution rather than app behavior, pin the default test host to `127.0.0.1` and keep host overrides opt-in via env vars.
#

# 2026-03-11
# Summarize inline results assistant shipped: `/summarize` now uses a wider two-zone layout so the composer and generated summary stay in the main reading path, while output-language, setup, and ZIP import controls move into a support rail. The summary card now promotes calendar/todo/share actions above the detailed sections, and the `Ask FAZUMI` panel uses more parent-specific BETA prompts derived from the current summary only.
# Files: specs/summarize-inline-results-assistant-2026-03-11.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, app/(dashboard)/summarize/page.tsx, components/SummaryDisplay.tsx, components/summary/FollowUpPanel.tsx
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: when the result is the product payoff, keep the generated answer and its action buttons in the first reading path; alternate setup and import tools belong in a secondary rail so they do not bury first value.
#

# 2026-03-11
# Summarize invalid-model fallback shipped: live production smoke on `POST /api/demo/summarize` initially reproduced the user-reported 500, Vercel logs exposed `Error: 400 invalid model ID`, and the shared summarize helper now retries once with `gpt-4o-mini` before failing. Production `OPENAI_MODEL` was reset to `gpt-4o-mini`, then the fix was redeployed and verified live on `https://fazumi.com`.
# Files: specs/summarize-invalid-model-fallback-2026-03-11.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, lib/ai/openai-chat.ts, lib/ai/summarize.ts, lib/ai/summarize-zip.ts
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` pass (`32 passed`, `3 skipped`). Production deploy `https://fazumi-whatsapp-summarizer-8ya939r2f-founder-fazumis-projects.vercel.app` is aliased to `https://fazumi.com`, and live `POST https://fazumi.com/api/demo/summarize` returns summary JSON.
# Pattern: summarize routes need both request-shape compatibility and a model-ID guardrail, because a bad deployment env value can otherwise break the landing demo and authenticated summarize flow at the same time.
#

# 2026-03-10
# Summarize OpenAI model compatibility hotfix implemented: text and ZIP summarization now share one OpenAI JSON chat-completion builder, use `max_completion_tokens` instead of deprecated `max_tokens`, and omit `temperature` for GPT-5 / o-series chat models so production summarize does not break when `OPENAI_MODEL` points at a reasoning family.
# Files: specs/summarize-openai-model-compat-hotfix-2026-03-10.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, lib/ai/openai-chat.ts, lib/ai/summarize.ts, lib/ai/summarize-zip.ts
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass.
# Pattern: summarize routes should build OpenAI chat requests through one shared compatibility layer so a model-family change cannot silently break the landing demo and authenticated summarize flow at the same time.
#

# 2026-03-10
# Profile identity sync + avatar upload completed: `/dashboard` now prefers the saved `profiles.full_name` value for its greeting, `/settings` replaces the avatar URL field with a click-to-upload device photo flow, and the shared top bar plus dashboard banner react immediately to profile update events without requiring logout/login.
# Files: specs/profile-identity-sync-avatar-upload-2026-03-10.md, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt, lib/profile-events.ts, app/(dashboard)/dashboard/page.tsx, components/dashboard/DashboardBanner.tsx, components/layout/TopBar.tsx, components/settings/SettingsPanel.tsx, app/api/profile/route.ts, app/api/profile/avatar/route.ts
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: when profile identity is shown in both server-rendered dashboard content and client shell chrome, read the server dashboard name from `profiles` for freshness, mirror the same fields into auth metadata for shell consumers, and broadcast a small client event after saves/uploads so mounted UI updates immediately.
#

# 2026-03-10
# Forgot-password flow completed: `/login` now includes a bilingual recovery request path with generic non-enumerating success copy, `/reset-password` handles the Supabase recovery redirect, clears the recovery hash, validates the new password, signs the user out globally, and returns them to `/login?reset=success`.
# Files: specs/forgot-password-flow-2026-03-10.md, app/login/page.tsx, app/reset-password/layout.tsx, app/reset-password/page.tsx, app/auth/callback/route.ts, e2e/app-smoke.spec.ts, e2e/support.ts, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`32 passed`, `3 skipped`).
# Pattern: Supabase password recovery must be treated as its own browser-handled redirect flow instead of assuming it will reuse the server-side OAuth callback contract.
#

# 2026-03-10
# Payment CTA copy refinement completed: the landing pricing block plus `/pricing` and `/billing` now show exact `Coming soon` / `قريبًا` copy on purchase buttons, and the shared checkout gate still prevents any Lemon Squeezy checkout redirect while paid-account management links stay unchanged.
# Files: specs/payment-cta-copy-refinement-2026-03-10.md, tasks/todo.md, tasks/lessons.md, docs/decisions.md, lib/payments-ui.ts, components/billing/CheckoutButton.tsx, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass.
# Pattern: when provider approval is still pending, acquisition CTAs should use explicit non-action copy instead of action text plus a suffix.
#

# 2026-03-10
# Payments coming-soon gate completed: added a shared payment-UI gate, disabled public checkout CTAs, and relabeled upgrade/view-plan acquisition surfaces with `(coming soon)` / `(قريبًا)` while keeping existing paid-user management links unchanged.
# Files: specs/payments-coming-soon-gate-2026-03-10.md, tasks/todo.md, tasks/lessons.md, docs/decisions.md, lib/payments-ui.ts, components/billing/CheckoutButton.tsx, components/landing/Pricing.tsx, components/billing/BillingPlansPanel.tsx, components/dashboard/DashboardBanner.tsx, components/layout/Sidebar.tsx, components/landing/GoToAppButton.tsx, components/SummaryDisplay.tsx, app/(dashboard)/summarize/page.tsx, components/founder-support/FounderSupportPage.tsx, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`31 passed`, `3 skipped`). Playwright needed an outside-sandbox rerun so `pnpm dev` could start normally.
# Pattern: when payment-provider approval is still pending, keep plan discovery visible but gate every acquisition CTA through one shared helper so pricing, founder, dashboard, and summarize surfaces cannot drift into "payments are live" copy.
#

# 2026-03-10
# Stale branch cleanup completed: deleted local and remote `feat/incremental-zip-summarize`, which was still pointing at commit `a9b5873` with vulnerable `next@15.4.0`, so the Vercel CVE-2025-66478 build failure is resolved without any app code changes.
# Files: scripts/ralph/progress.txt
# Verification: `git branch -a` no longer lists `remotes/origin/feat/incremental-zip-summarize`; `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`31 passed`, `3 skipped`, `0 failed`).
# Pattern: when a deploy is targeting a stale branch with no unique commits beyond `main`, remove the branch instead of patching code or duplicating already-shipped dependency upgrades.
#

# 2026-03-10
# Linux build script portability hotfix completed: the auto-run Next.js cleanup path now removes `.next/trace` and stale `.next/dev` artifacts through a shared Node helper, and both `predev` and `prebuild` no longer depend on `pwsh`, so Linux/Vercel builds and post-test local builds can reach `next build` normally.
# Files: specs/linux-build-script-portability-2026-03-10.md, package.json, scripts/clean-next-artifacts.mjs, tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass.
# Pattern: any npm script that runs automatically in build or dev startup must use a cross-platform helper, while PowerShell stays reserved for explicit Windows operator commands.
#

# 2026-03-10
# Summarize resilience hotfix completed: text and ZIP summary saves now retry without newer optional summary columns when the DB schema lags, AI-request logging and todo seeding no longer abort the request on thrown transport failures, and `/history` now tolerates missing optional summary metadata fields.
# Files: app/api/summarize/route.ts, lib/server/summaries.ts, app/(dashboard)/history/page.tsx, tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass (`31 passed`, `3 skipped`).
# Pattern: successful summaries must be decided by the core AI + save path only; optional metadata columns and best-effort follow-up writes must degrade safely instead of surfacing a generic 500.
#

# 2026-03-10
# Emotional design Phase 6 analytics alignment completed: the summarize flow now records the requested `first_value_delivered` session-start metric, the result/paywall/PMF surfaces emit the missing Phase 6 analytics events, and the re-engagement cron route logs `reengagement_sent` server-side without importing browser analytics code.
# Files: specs/emotional-design-phase-6-analytics-alignment-2026-03-10.md, tasks/todo.md, tasks/emotional-design-impl.md, tasks/lessons.md, lib/analytics.ts, app/(dashboard)/summarize/page.tsx, components/summary/StatusLine.tsx, components/SummaryDisplay.tsx, components/pmf/PmfSurveyModal.tsx, components/dashboard/UpgradeBanner.tsx, app/api/cron/reengagement/route.ts, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. Event firing was verified by code-path review in this shell environment rather than a live browser/PostHog session.
# Pattern: when an analytics phase specifies exact event names and fire points, replace the legacy alias and audit every named component or route directly instead of assuming the broad earlier instrumentation is sufficient.
#

# 2026-03-10
# Emotional design Phase 5 alignment completed: the morning digest now speaks in 7-day summary and action-item totals with a no-activity fallback, re-engagement moved into its own protected hourly cron route with verified-email and 28-day cooldown checks, and the PMF modal now shows the requested second-screen `biggest_benefit` follow-up only for `very_disappointed`.
# Files: specs/emotional-design-phase-5-retention-alignment-2026-03-10.md, tasks/todo.md, tasks/emotional-design-impl.md, tasks/lessons.md, lib/push/server.ts, scripts/schedule-morning-digest.ts, app/api/cron/reengagement/route.ts, components/pmf/PmfSurveyModal.tsx, vercel.json, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. Browser-level PMF submission and live cron execution were not run in this shell environment.
# Pattern: when a retention feature shares infrastructure with another scheduler, keep the delivery window logic reusable but give the feature its own protected cron boundary so copy, cooldowns, and user-level tracking can match the exact prompt without piggyback drift.
#

S001 = PENDING  # Next.js scaffold + dev tooling
S002 = PENDING  # Supabase DB schema + RLS
S003 = PENDING  # Paste text → AI summary → display
S004 = PENDING  # File upload (txt + zip)
S005 = PENDING  # Google + Apple OAuth
S006 = PENDING  # Persist summaries + usage tracking
S007 = PENDING  # Dashboard: history list + detail view
S008 = PENDING  # Lemon Squeezy payments + webhook
S009 = PENDING  # Plan limits enforcement
S010 = PENDING  # Referral system

# 2026-03-10
# Emotional design Phase 4 alignment completed: the summarize result now shows a quiet saved-school-context note when profile family memory exists, and the settings family-memory save action now confirms that future summaries will reflect the saved context without adding a toast or changing unrelated settings feedback.
# Files: components/SummaryDisplay.tsx, app/(dashboard)/summarize/page.tsx, components/settings/SettingsPanel.tsx, tasks/emotional-design-impl.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when saved context affects model quality but the AI contract does not expose a match flag, surface the investment with an app-side signal from already-loaded profile data instead of broadening the prompt or response schema.
#

# 2026-03-10
# Emotional design Phase 3 alignment completed: the dashboard upgrade banner now uses the requested Pro, Founder, and fallback welcome copy with a small `/summarize` link, and the summarize page now shows the exact DAILY_CAP / LIFETIME_CAP reassurance-plus-benefit copy plus a mobile near-limit indicator driven by live usage data.
# Files: components/dashboard/UpgradeBanner.tsx, app/(dashboard)/dashboard/page.tsx, app/(dashboard)/summarize/page.tsx, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when post-checkout UX depends on a webhook that may not have finished yet, pass the best available entitlement into the banner and keep the fallback useful instead of inventing plan certainty.
#

# 2026-03-09
# Emotional design Phase 1 completed: the summary result now adds a calm payoff block, a StatusLine built from existing structured summary data, visible family-context chips when saved memory is present, and a first-summary-only `time_to_first_value_recorded` analytics event from the summarize flow.
# Files: components/SummaryDisplay.tsx, app/(dashboard)/summarize/page.tsx, lib/analytics.ts, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when the emotional gap is on the result screen, prefer reusing already-structured summary metadata and prompt-context echoes instead of changing the AI contract or weakening the privacy promise.
#

# 2026-03-09
# Emotional design Phase 2 completed: the post-payment banner now explains the unlocked family benefits, the zero-history page teaches the first-use loop with a stronger CTA, and summarize limit messaging now distinguishes free-cap upgrade benefits from paid-plan daily limits.
# Files: components/dashboard/UpgradeBanner.tsx, app/(dashboard)/history/page.tsx, app/(dashboard)/summarize/page.tsx, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass.
# Pattern: when an emotional-state fix sits on an already-working flow, keep the behavior stable and change the framing first so users understand the benefit without retraining the route.
#

# 2026-03-09
# Emotional design Phase 3 completed: the result screen now acknowledges value at key saved-summary thresholds and adds contextual nudges toward calendar export, family sharing, and searchable history without introducing any new product surface.
# Files: components/SummaryDisplay.tsx, app/(dashboard)/summarize/page.tsx, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: discovery nudges work better when they are attached to the exact summary data that makes the next action useful, instead of being broadcast as generic feature marketing.
#

# 2026-03-09
# Emotional design Phase 4 completed: the dashboard now compares current stats to the prior week and includes a small in-product weekly progress summary built from existing summary history, without extending push delivery yet.
# Files: app/(dashboard)/dashboard/page.tsx, components/dashboard/DashboardBanner.tsx, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: progress feedback feels calmer when totals stay stable and the trend context explains the movement, instead of replacing the main metric with a trend-only view.
#

# 2026-03-09
# Emotional design Phase 5 completed: the PMF modal keeps its existing one-question flow, but "Very disappointed" responders now have a short follow-up field whose `missing_if_gone` answer is submitted through the already-supported PMF API contract.
# Files: components/pmf/PmfSurveyModal.tsx, app/api/pmf/route.ts, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when the backend contract already supports richer feedback, finish the client-side wiring first and avoid broadening the slice into redundant API changes.
#

# 2026-03-09
# Emotional design Phase 6 completed: the existing 7 AM digest channel now sends a Sunday weekly-progress recap instead of a second daily notification, uses dedicated morning/weekly digest timestamps for safe dedupe, and updates the settings copy so the push opt-in remains explicit.
# Files: supabase/migrations/2026030902_add_digest_delivery_tracking_to_push_subscriptions.sql, lib/push/server.ts, components/settings/SettingsPanel.tsx, scripts/schedule-morning-digest.ts, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a new notification would otherwise double-send on the same day, fold it into the existing cadence window and separate the delivery timestamps instead of stacking another push path on top.
#

# 2026-03-09
# Emotional design Phase 7 completed: users who have gone 14 or more days without a saved summary can now receive one calm re-engagement reminder per inactivity gap, tracked at the profile level and routed through the existing 7 AM notification window with explicit settings disclosure.
# Files: supabase/migrations/2026030903_add_profile_reengagement_tracking.sql, lib/push/server.ts, components/settings/SettingsPanel.tsx, scripts/schedule-morning-digest.ts, tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when the product promise is once per inactivity gap, track the reminder on the user record rather than the browser subscription so multi-device opt-in does not turn into duplicate nudges.
#

# 2026-03-09
# Emotional design Phase 8 completed: the rollout is now fully logged, `docs/decisions.md` records the final notification-cadence architecture, `tasks/lessons.md` captures the notification-scope mistake, and the final verification pass ended with green lint/typecheck/build plus a recorded Playwright server-start blocker.
# Files: tasks/emotional-design-impl.md, tasks/todo.md, tasks/lessons.md, docs/decisions.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass. `pnpm test` was attempted twice but the Playwright suite could not reach `http://localhost:3000` and failed with repeated `ECONNREFUSED ::1:3000`; `pwsh ./scripts/smoke.ps1` also timed out before returning a stable suite result.
# Pattern: when final verification fails in generated build artifacts or browser harness startup rather than app source, fix the generated-state issue directly, rerun the static checks, and record the exact remaining environment blocker instead of smearing it across the feature summary.
#

# 2026-03-09
# Founder supporter discoverability slice is code-complete: the desktop landing nav now includes an amber localized founder pill linking to `/founder-supporter`, and the Fazumi footer group adds a localized Founder Supporter link after Pricing on both desktop and mobile accordion layouts.
# Files: specs/founder-supporter-nav-footer-entry-2026-03-09.md, tasks/todo.md, components/landing/Nav.tsx, components/landing/Footer.tsx, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass. A temporary `pnpm start -- --port 3100` smoke setup reaches `Ready`, but the requested desktop/mobile/Arabic browser smoke is blocked in this sandbox because Playwright Chromium launch fails with `spawn EPERM`; an outside-sandbox rerun was requested.
# Pattern: when a public discoverability tweak still needs real browser confirmation, keep the UI slice shipped behind green static checks and record the exact sandboxed browser blocker instead of pretending the responsive smoke passed.
#

# 2026-03-09
# Founder support transparency page completed: added a hidden noindex `/founder-support` route with a bilingual founder note, soft placeholder photography blocks, trust-focused FAQ/CTA sections, and a new transparency link from `/founder-supporter` back into the anchored founder plan.
# Files: specs/founder-support-transparency-page-2026-03-09.md, tasks/todo.md, docs/decisions.md, app/founder-support/page.tsx, components/founder-support/FounderSupportPage.tsx, components/founder-offer/content.ts, components/founder-offer/FounderOfferPage.tsx, e2e/public-routes.spec.ts, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass. `pnpm test` was attempted twice but remains blocked locally because the Playwright app server drops into `ECONNRESET` / `ECONNREFUSED`, and a targeted rerun then failed with `spawn EPERM`.
# Pattern: when a campaign page needs a deeper trust layer, keep the explanation on a separate noindex route and link back into the main offer with anchored CTAs instead of expanding the primary conversion page into a long appendix.
#

# 2026-03-09
# Founder supporter hardening completed: `/founder-supporter` now forces dynamic rendering so the server-side Supabase cookie read stays request-scoped in production, and `tasks/todo.md` was reconciled to the shipped implementation state for the ALB and SEO2 checklist items.
# Files: app/founder-supporter/page.tsx, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass for this patch set.
#

# 2026-03-09
# Admin polish slice completed: `/admin_dashboard/login` now owns the centered route shell with the top-right theme switcher, the login card shows the brand mark plus the muted dev-only access note, the five main admin dashboard pages render restored server-safe breadcrumbs through `AdminShell`, and the inbox item panel localizes its status, priority, and tag controls in both EN and AR.
# Files: specs/admin-login-breadcrumb-bilingual-2026-03-09.md, tasks/todo.md, scripts/ralph/progress.txt, app/admin_dashboard/login/page.tsx, app/admin_dashboard/(dashboard)/page.tsx, app/admin_dashboard/(dashboard)/users/page.tsx, app/admin_dashboard/(dashboard)/income/page.tsx, app/admin_dashboard/(dashboard)/ai-usage/page.tsx, app/admin_dashboard/(dashboard)/inbox/page.tsx, components/admin/AdminLoginScreen.tsx, components/admin/AdminBreadcrumb.tsx, components/admin/AdminInboxItemPanel.tsx, tasks/lessons.md
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass. The first `pnpm build` attempt hit a stale `.next/lock`; rerunning immediately after the lock cleared succeeded.
# Pattern: when a shell slot is added ahead of page wiring, keep the shared slot component around until the route-level integration lands; otherwise later polish passes degrade into restore work instead of straightforward wiring.
#
# 2026-03-09
# Founder supporter public landing page completed: added a new public `/founder-supporter` route with structured founder-offer copy, premium mobile-first sections, a tasteful sticky mobile CTA, sitemap inclusion, and a dedicated public-route Playwright smoke assertion while keeping the existing logged-in `/founder` member page untouched.
# Files: specs/founder-supporter-public-landing-2026-03-09.md, docs/decisions.md, tasks/todo.md, app/founder-supporter/page.tsx, app/sitemap.ts, components/founder-offer/content.ts, components/founder-offer/FounderOfferPage.tsx, e2e/public-routes.spec.ts, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` all pass.
# Pattern: when a campaign landing page shares a slug family with an existing logged-in route, create a separate public path and keep all offer copy plus CTA wiring in one dedicated module so marketing can move quickly without destabilizing in-app founder UX.

# 2026-03-09
# Admin inbox item panel completed: the inbox now opens a dedicated accordion-based detail panel with localized status, priority, tags, and admin-notes editing, shows list-plus-panel on desktop, swaps list for panel on mobile, and saves through `PATCH /api/admin/inbox` with inline saved feedback.
# Files: specs/admin-inbox-item-panel-2026-03-09.md, tasks/todo.md, tasks/lessons.md, docs/decisions.md, scripts/ralph/progress.txt, components/admin/AdminInboxContent.tsx, components/admin/AdminInboxItemPanel.tsx, components/ui/accordion.tsx, components/ui/select.tsx, components/landing/FAQ.tsx, components/landing/FAQAccordion.tsx, components/founder-offer/FounderOfferPage.tsx, app/help/page.tsx
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a new admin UI slice needs richer compound primitives than the live shared helper exposes, add the local primitive surface and keep explicit legacy exports so older screens can migrate intentionally instead of breaking all at once.
#
# 2026-03-09
# Admin users actions + churn status completed: the admin users roster now shows a recent-activity avatar stack, bilingual churn-risk badges, and a row-level actions menu for plan changes, trial extension, ban, and reset-ban. This run also upgraded the shared dropdown-menu surface for the existing top bar and restored the billing smoke selectors that verification depends on.
# Files: specs/admin-users-actions-churn-2026-03-09.md, tasks/todo.md, components/admin/AdminAvatarStack.tsx, components/admin/ChurnRiskBadge.tsx, components/admin/AdminUsersTable.tsx, app/api/admin/users/plan-change/route.ts, app/api/admin/users/trial-extend/route.ts, components/ui/dropdown-menu.tsx, components/layout/TopBar.tsx, components/dashboard/UpgradeBanner.tsx, app/(dashboard)/billing/page.tsx, tasks/lessons.md
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` all pass.
# Pattern: when a dashboard slice depends on shared UI primitives or existing Playwright contracts, upgrade the shared primitive once and preserve the mounted selector contract instead of patching tests around UI drift.
#
# 2026-03-09
# Admin income pricing tiers completed: the income dashboard now opens with a four-card Free/Monthly/Annual/Founder tier strip, animated MRR counts, a highest-MRR popular badge, and a founder seat-cap progress bar while staying UI-only against the current admin income type drift.
# Files: components/admin/AdminPricingTiers.tsx, components/admin/AdminIncomeContent.tsx, specs/admin-income-pricing-tiers-2026-03-09.md, tasks/todo.md
# Verification: `pnpm lint` and `pnpm typecheck` pass. `pnpm test` was attempted twice in this shell and timed out before a stable pass/fail result returned.
# Pattern: when a requested dashboard UI slice targets a newer payload than the checked-in type exposes, normalize both shapes locally in the component instead of broadening the slice into admin queries or shared types.

# 2026-02-28
# Hydration mismatch fix completed for theme/lang client preferences and browser-injected attributes.
# Files: app/layout.tsx, public/hydration-sanitize.js, lib/context/ThemeContext.tsx, lib/context/LangContext.tsx, lib/hooks/useMounted.ts, components/landing/Nav.tsx, components/layout/TopBar.tsx, tasks/lessons.md
# Pattern: use useSyncExternalStore for browser preference state that must match SSR on the first hydration pass.

# 2026-02-28
# Product loop bugfixes: summarize persistence is explicit, daily limits use one UTC key, dashboard widgets/notifications now derive from saved summaries, and local dev helpers can seed test accounts/set plans.
# Files: app/api/summarize/route.ts, lib/limits.ts, lib/dashboard-insights.ts, lib/hooks/useDashboardInsights.ts, components/widgets/CalendarWidget.tsx, components/widgets/TodoWidget.tsx, components/layout/TopBar.tsx, components/dashboard/DashboardBanner.tsx, app/summarize/page.tsx, app/api/dev/create-test-accounts/route.ts, app/api/dev/set-plan/route.ts, README.md, tasks/lessons.md
# Pattern: for any product loop that depends on persistence, drive UI from saved rows rather than sample data and fail loudly when the write path breaks.

# 2026-02-28
# Lemon Squeezy env naming is standardized around canonical names with a legacy webhook-secret fallback so local replay and production config resolve the same values.
# Files: app/api/webhooks/lemonsqueezy/route.ts, scripts/webhooks/replay-lemonsqueezy.js, scripts/webhooks/README.md, README.md, .env.local.example
# Pattern: when env names drift, keep one canonical name in docs and accept a narrow legacy alias only at the boundary that reads the value.

# 2026-03-01
# Reliability bolt-ons added: Sentry error monitoring for App Router, structured JSON server logs for summarize/webhook routes, and a tighter health response plus deploy smoke checklist.
# Files: app/api/summarize/route.ts, app/api/webhooks/lemonsqueezy/route.ts, app/api/health/route.ts, app/global-error.tsx, instrumentation.ts, instrumentation-client.ts, sentry.server.config.ts, sentry.edge.config.ts, lib/logger.ts, lib/sentry.ts, next.config.ts, README.md, .env.local.example
# Pattern: for pre-production observability, keep capture payloads intentionally sparse by hashing identifiers, redacting content-like keys, and logging route-safe metadata only.

# 2026-03-02
# OAuth flow hardened: login now preserves `next` through Google/Apple and email callback URLs, uses an explicit browser redirect from the returned provider URL, and the auth callback normalizes the post-login destination across proxied hosts.
# Files: app/login/page.tsx, app/auth/callback/route.ts, tasks/lessons.md
# Pattern: when an auth flow crosses origins, always carry a sanitized relative return path through the callback and do not rely on implicit provider redirects alone.

# 2026-03-02
# Dev-only admin dashboard added with overview, users, income, guarded admin APIs, and polling-based recent subscription notifications backed by the Supabase service role.
# Files: app/admin_dashboard/**, app/api/admin/**, components/admin/**, lib/admin/**, lib/supabase/admin.ts, lib/supabase/middleware.ts, .env.local.example, docs/decisions.md
# Pattern: keep internal operator tooling behind a development-only cookie gate and centralize all service-role access in server-side helpers so pages and APIs share one audited path.

# 2026-03-03
# Landing conversion slice completed: larger Apple-style typography, rotating hero headline, interactive gated demo, and How It Works screenshot placeholders with updated copy.
# Files: app/globals.css, components/landing/Hero.tsx, components/landing/HowItWorks.tsx, components/landing/Pricing.tsx, components/landing/FAQAccordion.tsx, components/ui/accordion.tsx, specs/hero-redesign-typography-bv4-bv6.md, tasks/todo.md
# Pattern: keep landing demos local and deterministic so users can preview the product without adding backend/storage complexity or risking raw chat persistence.

# 2026-03-03
# Typography overhaul landed: Inter + Cairo font loading, corrected mobile/desktop scale tokens, Arabic line-height refinements, and consistent 16px input/form text across landing, dashboard, history, and auth/contact surfaces.
# Files: app/globals.css, app/layout.tsx, app/dashboard/page.tsx, app/login/page.tsx, app/summarize/page.tsx, components/contact/ContactForm.tsx, components/dashboard/DashboardBanner.tsx, components/history/HistoryList.tsx, components/landing/Hero.tsx, components/landing/HowItWorks.tsx, components/landing/Pricing.tsx, components/landing/FAQAccordion.tsx, components/landing/FAQ.tsx, components/landing/GoToAppButton.tsx, components/landing/Newsletter.tsx, components/landing/Testimonials.tsx, components/ui/accordion.tsx, components/ui/input.tsx, components/ui/textarea.tsx, specs/typography-overhaul-2026-saas-standards.md, tasks/todo.md
# Pattern: when typography drifts across a bilingual product, fix the root tokens and shared primitives first, then apply explicit component-level sizes only where the hierarchy must differ from the global defaults.

# 2026-03-03
# Launch blocker fix completed: the global App Router error boundary is now hook-free, root themeColor moved to viewport, and production verification is green without the prior metadata warnings.
# Files: app/global-error.tsx, app/layout.tsx, specs/launch-blockers-p0-fix.md, tasks/todo.md, tasks/lessons.md
# Pattern: for special Next.js boundary files, prefer self-contained markup first, then verify the live build output before applying broader cleanup work.

# 2026-03-03
# Follow-up build blocker verification completed: clearing `.next` and `node_modules/.cache` plus `pnpm store prune` removed the stale-failure state, and the clean build/lint/typecheck pass without extra app-code changes.
# Files: specs/build-errors-p0-fix-2.md, specs/launch-blockers-p0-fix.md, tasks/todo.md, scripts/ralph/progress.txt
# Pattern: when a Next.js production failure disappears after a clean cache reset, record the clean-run evidence before rewriting boundary components or route pages.

# 2026-03-03
# Public-route prerender hardening completed: added a custom App Router 404 page and defaulted localized section item arrays to [] before rendering list lengths/maps on legal/help pages.
# Files: app/not-found.tsx, app/cookie-policy/page.tsx, app/privacy/page.tsx, app/terms/page.tsx, app/help/page.tsx, tasks/todo.md, scripts/ralph/progress.txt, tasks/lessons.md
# Build status: `pnpm lint`, `pnpm typecheck`, and `pnpm build` all pass; build reached `Generating static pages using 7 workers (36/36)`. `pnpm test` is currently blocked by Playwright webServer startup timing out after port 3000 was already occupied.
# Pattern: for localized collections rendered in SSR paths, coerce the selected value to a concrete array before any `.length` or `.map` access instead of relying on the localization helper to always return one.

# 2026-03-03
# Final launch-blocker pass completed: cleared `.next` and `node_modules/.cache`, pruned the pnpm store, replaced `app/error.tsx` with inline SSR-safe route boundary UI, and re-audited the legal/help pages for concrete array normalization.
# Files: app/error.tsx, specs/launch-blockers-final-fix.md, tasks/todo.md, scripts/ralph/progress.txt, tasks/lessons.md
# Build status: `pnpm lint`, `pnpm typecheck`, and a clean `pnpm build` pass; build reached `Generating static pages using 7 workers (36/36)` with exit code `0`. `pnpm test` first failed because a stale Next.js server held port `3000`, then timed out on retry after the process was stopped.
# Pattern: treat `app/error.tsx` as a special App Router boundary and keep it self-contained so route fallback rendering never depends on provider context during prerender.

# 2026-03-03
# Next.js version rollback verification completed: pinned `next` and `eslint-config-next` to `16.0.0`, refreshed the lockfile, and recorded the downgrade run in a dedicated spec/checklist.
# Files: package.json, pnpm-lock.yaml, specs/nextjs-version-fix.md, tasks/todo.md, scripts/ralph/progress.txt, tasks/lessons.md
# Build status: two clean `pnpm build` runs pass on Next.js `16.0.0` and reach `Generating static pages (36/36)` with exit code `0`; `pnpm lint` and `pnpm typecheck` pass. `pnpm test` is still blocked by `e2e/app-smoke.spec.ts` timing out while waiting for `summary-use-sample`.
# Pattern: when a production build failure points at framework internals, pin the framework version first and verify with clean caches before adding app-side suppression or route-level workarounds.

# 2026-03-07
# Audit blocker pass in progress: admin auth now fails closed without explicit credentials, shared billing entitlements derive from subscription state first, Arabic-first public route coverage is explicit, and /status no longer claims live operational health.
# Files: lib/admin/auth.ts, app/api/admin/login/route.ts, app/api/webhooks/lemonsqueezy/route.ts, lib/limits.ts, app/api/summarize/route.ts, app/api/summarize-zip/route.ts, app/billing/page.tsx, app/history/[id]/page.tsx, app/summarize/page.tsx, app/page.tsx, components/landing/GoToAppButton.tsx, app/status/page.tsx, e2e/admin-dashboard.spec.ts, e2e/public-routes.spec.ts, docs/decisions.md, docs/runbooks/payments.md, tasks/todo.md
# Pattern: when access control depends on mirrored billing/profile fields, resolve entitlements from one shared subscription-aware helper and keep every UI mirror fail-safe instead of letting stale rows grant access.

# 2026-03-07
# Verification update: `pnpm lint`, `pnpm typecheck`, and `pnpm build` now pass after the blocker patch set. `pnpm exec playwright test e2e/public-routes.spec.ts` also passes for the locale/status smoke path. The full `pnpm test` suite is still blocked locally by Playwright instability (`port 3100 already used` on one run, then worker `spawn EPERM` / timeout on follow-up runs).
# Files: tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt
# Pattern: when the full browser suite is unstable locally, record the exact failing runner condition separately from the code fixes so product risk and environment risk do not get conflated.

# 2026-03-07
# SEO hardening pass in progress: production canonicals/hreflang metadata, route-level metadata layouts, structured data expansion, sitemap/robots updates, and crawlable SSR output for `/about`.
# Files: app/layout.tsx, app/page.tsx, app/pricing/page.tsx, app/sitemap.ts, app/robots.ts, app/about/page.tsx, app/pricing/layout.tsx, app/about/layout.tsx, app/faq/layout.tsx, app/contact/layout.tsx, app/terms/layout.tsx, app/privacy/layout.tsx, app/help/layout.tsx, app/cookie-policy/layout.tsx, app/refunds/layout.tsx, app/status/layout.tsx, tasks/todo.md, scripts/ralph/progress.txt
# Pattern: when locale is cookie-based without URL prefixes, keep canonicals stable on the public path and expose language alternates from metadata rather than duplicating routes.

# 2026-03-07
# SEO hardening Round 2 completed: fixed homepage schema drift, added Twitter handle metadata and keyword coverage, enriched pricing product schema, made `/about` SSR cookie-aware for locale/RTL, and prioritized the first How It Works illustration for LCP.
# Files: app/page.tsx, app/layout.tsx, app/pricing/page.tsx, app/about/page.tsx, components/landing/HowItWorks.tsx, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. `pnpm test` was attempted twice in local CI mode and timed out instead of producing a reliable pass/fail result.
# Pattern: when SEO hardening depends on locale-specific rendering, use the same server cookie source as the root layout so SSR language, `dir`, and schema metadata do not drift from the hydrated app state.

# 2026-03-07
# SEO hardening Round 3 completed: restored the confirmed homepage social profiles and GCC geography signals, aligned the PWA manifest with the Arabic-first summarize flow, added FAQ/About JSON-LD at the route boundary, and stopped globally preloading Alexandria.
# Files: app/page.tsx, public/manifest.json, app/faq/page.tsx, app/about/page.tsx, app/faq/layout.tsx, app/layout.tsx, tasks/todo.md, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a route already depends on client-only locale state, emit static JSON-LD with inline `<script type="application/ld+json">` blocks at the page/layout boundary instead of rewriting the route just for SEO metadata.

# 2026-03-07
# SEO hardening Round 4 completed: added noindex route layouts for logged-in surfaces, kept the admin dashboard layout behavior intact while blocking indexing, strengthened help/status internal linking, and upgraded `/help` plus `/status` layouts to `WebPage` JSON-LD wrappers.
# Files: app/login/layout.tsx, app/dashboard/layout.tsx, app/summarize/layout.tsx, app/history/layout.tsx, app/billing/layout.tsx, app/settings/layout.tsx, app/profile/layout.tsx, app/calendar/layout.tsx, app/admin_dashboard/(dashboard)/layout.tsx, app/help/page.tsx, app/status/page.tsx, app/help/layout.tsx, app/status/layout.tsx, specs/seo-round-4-noindex-internal-links.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a route should remain functional but stay out of search results, add route-level metadata in a thin App Router layout instead of threading robots flags through unrelated page logic.

# 2026-03-08
# SEO hardening Round 6 completed: the remaining public layouts now share hreflang alternates, and contact/help/status/legal layouts expose consistent `WebPage` plus `BreadcrumbList` JSON-LD wrappers with stable route canonicals.
# Files: app/about/layout.tsx, app/contact/layout.tsx, app/faq/layout.tsx, app/help/layout.tsx, app/status/layout.tsx, app/pricing/layout.tsx, app/terms/layout.tsx, app/privacy/layout.tsx, app/cookie-policy/layout.tsx, app/refunds/layout.tsx, specs/seo-round-6-public-layout-breadcrumbs.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when locale uses one canonical public path, expose language alternates at the metadata layer and reuse breadcrumb schema across static public layouts instead of inventing localized route variants.

# 2026-03-08
# SEO hardening Round 7 completed: the remaining public layouts now use keyword-rich absolute title tags, and a public `llms.txt` advertises Fazumi's scope, pricing, key pages, and privacy stance for AI retrieval.
# Files: app/about/layout.tsx, app/faq/layout.tsx, app/contact/layout.tsx, app/pricing/layout.tsx, app/help/layout.tsx, app/status/layout.tsx, public/llms.txt, specs/seo-round-7-absolute-titles-llms.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: when a route-specific title must ignore the shared root template, set `title.absolute` in the route layout and keep every other metadata field unchanged so canonicals/schema stay stable.

# 2026-03-08
# SEO hardening Round 8 completed: the remaining legal layouts now use explicit absolute titles, and the key public layouts expose route-level Open Graph metadata with relative URLs resolved by the root metadata base.
# Files: app/terms/layout.tsx, app/privacy/layout.tsx, app/cookie-policy/layout.tsx, app/refunds/layout.tsx, app/about/layout.tsx, app/faq/layout.tsx, app/pricing/layout.tsx, app/contact/layout.tsx, app/help/layout.tsx, app/status/layout.tsx, specs/seo-round-8-legal-titles-open-graph.md, tasks/todo.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass.
# Pattern: for static App Router layouts that already have stable canonicals, add route-level Open Graph metadata with the same path and keep `url` relative so `metadataBase` stays the single source of truth for the absolute domain.

# 2026-03-08
# Founder supporter UX phase 0 completed in-app: founder users now get a dedicated shell identity, billing thank-you state, corrected Arabic afternoon greeting window, and profile-name sync into Auth metadata so the mounted top bar refreshes without logout/login.
# Files: specs/founder-supporter-ux-phase-0.md, tasks/todo.md, components/layout/TopBar.tsx, components/dashboard/DashboardBanner.tsx, app/billing/page.tsx, lib/i18n.ts, app/api/profile/route.ts, components/settings/SettingsPanel.tsx, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. `pnpm test` still fails on existing Playwright cases outside this slice: `e2e/admin-dashboard.spec.ts`, `e2e/app-smoke.spec.ts`, `e2e/summarize-auth.spec.ts`, and `e2e/summarize-zip.spec.ts`. The founder-related billing smoke inside `e2e/app-smoke.spec.ts` ran before the failure set.
# Pattern: when a premium tier needs distinct recognition without new routes, keep entitlement logic unchanged and layer the identity through plan-specific copy, badges, and billing-state acknowledgements.

# 2026-03-08
# UX bug batch applied: `/summarize` now lands directly on the working card with a compact inline title and privacy note under the submit row, the summarize shell is wider on laptop widths, dashboard-shell routes are grouped under `app/(dashboard)` with a scoped fade-in template, and the dashboard/landing locale toggles now show `EN / عربي` while the top-bar action icons stay visible on small phones.
# Files: specs/ux-bugs-batch-2026-03-08.md, tasks/todo.md, app/(dashboard)/template.tsx, app/(dashboard)/dashboard/**, app/(dashboard)/summarize/**, app/(dashboard)/history/**, app/(dashboard)/billing/**, app/(dashboard)/settings/**, app/(dashboard)/profile/**, app/(dashboard)/calendar/**, app/(dashboard)/todo/**, components/layout/TopBar.tsx, components/landing/Nav.tsx, components/history/types.ts, components/history/HistoryList.tsx, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass. A real-browser smoke attempt was started, but local verification is still blocked here by Playwright browser `spawn EPERM` and follow-up `/summarize` navigation timeouts against the local dev server.
# Pattern: when a route-transition fix depends on App Router remount behavior, use a route group plus `template.tsx`, and keep shared types out of `app/**` so file moves do not break unrelated components.

# 2026-03-08
# Loading cleanup and founder story slice completed: the dashboard, billing, and summarize loading states are now plain first-card skeletons without mascot copy, summarize loading matches the live `max-w-4xl` width, and founders now have a noindex `/founder` dashboard route with the full story plus a billing-page link into it.
# Files: specs/loading-skeleton-founder-page-2026-03-08.md, tasks/todo.md, app/(dashboard)/dashboard/loading.tsx, app/(dashboard)/billing/loading.tsx, app/(dashboard)/summarize/loading.tsx, app/(dashboard)/founder/layout.tsx, app/(dashboard)/founder/page.tsx, app/(dashboard)/billing/page.tsx, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass after the loading cleanup and again after the founder page. Static grep verification confirms the three target loading files no longer reference `MascotArt` or `LocalizedText`. Browser/manual smoke is still pending in this environment.
# Pattern: treat dashboard loading files as structural placeholders for the live route, and keep founder storytelling in a dedicated noindex dashboard page linked from the founder billing state instead of expanding the billing card indefinitely.

# 2026-03-08
# Founder supporter UX phase 2 completed: Manrope now loads only the explicit shipped weights, Arabic shared text has roomier body and micro-text line-height, mobile tap flashes are suppressed, founders get a persistent `/founder` sidebar entry, and the dashboard shows a one-time founder welcome modal stored in browser localStorage.
# Files: specs/founder-supporter-ux-phase-2.md, tasks/todo.md, app/layout.tsx, app/globals.css, lib/i18n.ts, components/layout/Sidebar.tsx, components/founder/FounderWelcomeModal.tsx, app/(dashboard)/dashboard/page.tsx, docs/decisions.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint` and `pnpm typecheck` pass after the CSS section and again after the founder UI section. `pnpm build` passes. `pnpm test` still fails on pre-existing Playwright cases outside this slice: `e2e/admin-dashboard.spec.ts`, `e2e/app-smoke.spec.ts` summarize-save smoke, `e2e/summarize-auth.spec.ts`, and `e2e/summarize-zip.spec.ts`. Real-browser/manual founder smoke is still pending in this environment.
# Pattern: keep founder-specific acknowledgement inside the existing shell, and use a browser-local "seen" flag for one-time celebratory UI that does not need to sync across devices or touch the database.

# 2026-03-08
# Playwright alignment slice completed: the transparent Fazumi logo asset is ready for its own commit, the four pre-existing Playwright failures are aligned to current admin, locale, summarize, and ZIP behavior, and repo verification is green again.
# Files: specs/playwright-test-alignment-2026-03-08.md, tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt, e2e/admin-dashboard.spec.ts, e2e/app-smoke.spec.ts, e2e/summarize-auth.spec.ts, e2e/summarize-zip.spec.ts, public/brand/logo/Fazumi logo transparent.png
# Verification: each target spec passes individually, then `pnpm lint`, `pnpm typecheck`, and `pnpm test` all pass.
# Pattern: keep browser smoke tests aligned to current UI contracts with stable selectors and explicit seeded state when the test intent is not backend schema coverage.

# 2026-03-08
# P8 reliability slice completed: the deprecated root `middleware.ts` file was migrated to Next.js 16 `proxy.ts`, Playwright now supports `PLAYWRIGHT_NO_SERVER=1`, and a local PowerShell smoke runner can pre-start a local Next server, wait on `/api/health`, then execute the suite against the existing server.
# Files: proxy.ts, playwright.config.ts, lib/testing/playwright.ts, scripts/smoke.ps1, package.json, README.md, tasks/todo.md, tasks/lessons.md, scripts/ralph/progress.txt
# Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm build` pass after each P8 item; smoke helper is documented and ready for local use.
# Pattern: when local browser coverage flakes because the runner owns server startup, give Playwright an env-guarded attach mode and standardize one repo script around the app's health endpoint.

## Sprint: Founder UX + Pre-Launch Polish (2026-03-08)

### What changed
- Entitlement wiring: Sidebar, DashboardBanner, SettingsPanel all use resolveEntitlement()
- FounderWelcomeModal mounted on dashboard, summarize, and history routes
- Duplicate modal mount in history/page.tsx removed
- Profile page: settings link + mailto deletion link (GDPR path)
- FAQ: Arabic-support and privacy answers corrected to match implementation
- About page: unverified metrics flagged with TODO comments
- Summarize page: loading/error/limit copy aligned to voice.md
- robots.ts: /founder, /admin, /api, /calendar, /offline all blocked
- tasks/todo.md: T6 test note resolved, BV4-6, HR5-7, RC8 checked

### Files touched
components/layout/Sidebar.tsx, components/settings/SettingsPanel.tsx,
components/founder/FounderWelcomeModal.tsx, app/(dashboard)/dashboard/page.tsx,
app/(dashboard)/history/page.tsx, app/(dashboard)/summarize/page.tsx,
app/(dashboard)/profile/page.tsx, app/(dashboard)/billing/page.tsx,
app/(dashboard)/founder/page.tsx, components/landing/FAQAccordion.tsx,
app/about/page.tsx, app/robots.ts, e2e/public-routes.spec.ts, tasks/todo.md

### Patterns confirmed
- resolveEntitlement() is the single source of truth for billingPlan everywhere
- Founder badge / nav item / modal are all entitlement-driven, not profile.plan-driven
- FounderWelcomeModal localStorage guard prevents double-show across routes

## Hotfix: predev EPERM + smoke runner confirmation (2026-03-08)

### What changed
- predev script: removes stale `.next/cache/.previewinfo` on Windows before dev start
- L020 added to lessons: Next.js 16 `proxy.ts` convention confirmed correct

### Smoke result
- Timed out before completion in this Codex shell environment
- Observed before timeout: 5 failed (`admin routes require explicit configured credentials before the dashboard is reachable`, `auth + dashboard smoke: email login reaches dashboard`, `billing lifecycle smoke: replayed Lemon Squeezy webhooks update UI without checkout`, `public smoke: / renders with the Arabic-first default`, `public smoke: /pricing renders with the Arabic-first default`)
- Full pass/skip totals could not be verified here because backgrounded `next dev` hit `spawn EPERM` / `EPIPE` under local smoke automation

### Patterns confirmed
- proxy.ts is the Next.js 16 middleware convention (`PROXY_FILENAME = "proxy"`)
- pnpm dev EPERM on Windows can come from stale `.next/cache` cleanup or stale `.next/dev` worker state after a crashed run

## Story: Build Trace Cleanup + Group Name Memory (2026-03-08)

### What changed
- `package.json`: safe `Test-Path` guards now clear `.next/cache/.previewinfo` and `.next/trace` before dev, and `.next/trace` before build
- `lib/family-context.ts`: `FamilyContext` and `normalizeFamilyContext()` now include `group_names: string[]`
- `components/settings/SettingsPanel.tsx`: family memory adds a bilingual `School group names` textarea and saves newline-separated names into `family_context.group_names`
- `app/(dashboard)/summarize/page.tsx`: the existing group-name input now exposes saved `group_names` through a datalist suggestion list without adding a new query

### Files touched
package.json, lib/family-context.ts, components/settings/SettingsPanel.tsx,
app/(dashboard)/summarize/page.tsx, tasks/todo.md, tasks/lessons.md,
scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed after the build-script patch, after the family-context type patch, after the settings UI patch, after the summarize datalist patch, and again at the end
- `pnpm typecheck` passed after each of those same checkpoints
- `pnpm build` passed after each requested checkpoint with the stale `.next/trace` cleanup in place
- Final `pnpm test` passed in attach mode against a local dev server on port `3100`: 28 passed, 3 skipped

### Patterns confirmed
- PowerShell cleanup scripts for optional `.next` files must use `Test-Path` guards or they can fail npm pre-scripts even with `-ErrorAction SilentlyContinue`
- Saved family-context arrays are a good fit for reusable user-entered suggestion lists when the page already fetches `family_context` on mount

## Story: Group Name Prompt + Summary Tagging (2026-03-08)

### What changed
- `lib/family-context.ts`: saved `group_names` now count as real family-context signal and render into the AI prompt as `Known group names`
- `supabase/migrations/2026030801_add_group_name_to_summaries.sql`: adds `summaries.group_name`
- `app/api/summarize/route.ts` and `lib/server/summaries.ts`: text summaries now persist the selected `group_name` alongside the existing summary metadata
- `app/(dashboard)/history/page.tsx`, `components/history/types.ts`, and `components/history/HistoryList.tsx`: history reads `group_name` and shows it as a badge when present

### Files touched
lib/family-context.ts, supabase/migrations/2026030801_add_group_name_to_summaries.sql,
app/api/summarize/route.ts, lib/server/summaries.ts, app/(dashboard)/history/page.tsx,
components/history/types.ts, components/history/HistoryList.tsx, tasks/todo.md,
tasks/lessons.md, scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed after the family-context patch
- `pnpm typecheck` passed after the family-context patch
- `pnpm lint` passed after the storage/history patch
- `pnpm typecheck` passed after the storage/history patch
- Final `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` all passed
- History/save-path rollout safety was verified by handling a pre-migration `summaries.group_name` schema-cache miss during Playwright runs without regressing the new field when the column exists

### Patterns confirmed
- Family-context prompt signal must treat saved group names as first-class memory, not only teachers and recurring links
- Summary metadata fields need migration, write-path, and read-path updates in the same slice or history UI will silently drift from the request contract

## Sprint: Group name filter + detail badge (2026-03-08)

### What changed
- HistoryList: group filter dropdown (hidden when no group names are present)
- History detail page: group name badge below title
- Migration 2026030801 remote push attempted, but `supabase db push --include-all` was blocked by remote migration-history drift (`Remote migration versions not found in local migrations directory`)

### Files touched
components/history/HistoryList.tsx, app/(dashboard)/history/[id]/page.tsx

### Patterns confirmed
- Group filter is client-side and derived from the already-fetched `summaries` prop, so no extra Supabase query was added
- Detail badge reuses the existing group-name badge treatment with `bg-[var(--primary)]/10` and primary text
- Remote Supabase pushes can fail before applying new SQL when the remote migration history no longer matches the local migrations directory

## Story: Migration drift follow-up + ZIP metadata/docs (2026-03-08)

### What changed
- Ran the canonical Supabase repair sequence: the first `supabase db push --include-all` reproduced the known drift error, `supabase migration repair --status reverted 20260213 20260301 20260303` succeeded, and the second push is still blocked by the same remote-history drift with missing version `20260305`
- Added a top-level JSDoc block to `scripts/schedule-morning-digest.ts` and a new README `Scheduled Scripts` section for `pnpm push:morning-digest`
- Updated the ZIP summarize client to post `group_name` and the ZIP API route to accept that key, keep the legacy `group_key` fallback, and persist the normalized `groupTitle` through `saveSummaryRecord()`

### Files touched
scripts/schedule-morning-digest.ts, README.md, app/(dashboard)/summarize/page.tsx,
app/api/summarize-zip/route.ts, tasks/todo.md, tasks/lessons.md,
scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed after the digest-doc patch
- `pnpm typecheck` passed after the digest-doc patch
- `pnpm lint` passed after the ZIP group-name patch
- `pnpm typecheck` passed after the ZIP group-name patch
- Final `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` all passed
- `pnpm test` completed with 28 passed and 3 skipped

### Patterns confirmed
- If a shared summary field is introduced in the text flow, the ZIP FormData contract and shared persistence helper must be updated in the same slice
- Remote Supabase migration drift can partially repair and still remain blocked on another missing legacy timestamp, so the exact CLI output needs to be recorded before trying anything beyond the canonical sequence

## Sprint: Web push opt-in UI in settings (2026-03-08)

### What changed
- SettingsPanel: morning digest toggle (subscribe/unsubscribe lifecycle)
- app/api/push/unsubscribe/route.ts: confirmed existing route, no new file needed
- eslint.config.mjs: ignore generated Playwright report folders so repo-wide lint targets source files
- tasks/todo.md: WP5 marked done

### Files touched
components/settings/SettingsPanel.tsx,
eslint.config.mjs,
tasks/todo.md

## Sprint: PMF survey + dashboard group count (2026-03-08)

### What changed
- PmfSurveyModal: shows after the 3rd saved summary, asks the one-question PMF survey, and POSTs the selected response to `/api/pmf`
- DashboardBanner: 3rd stat added for active groups, while the summaries/time-saved stat row now uses all-time summary count
- `app/api/pmf/route.ts`: confirmed existing PMF API route and reused it instead of creating a duplicate endpoint

### Files touched
components/pmf/PmfSurveyModal.tsx,
app/(dashboard)/summarize/page.tsx,
app/(dashboard)/dashboard/page.tsx,
components/dashboard/DashboardBanner.tsx,
tasks/todo.md,
scripts/ralph/progress.txt

## Sprint: Admin orphan triage + settings timezone (2026-03-09)

### What changed
- Removed the orphaned untracked admin files `AdminBreadcrumb.tsx` and `AdminThemeSwitcher.tsx` after confirming nothing in `app/` or `components/` imports them
- Added `profiles.timezone` via `2026030901_add_timezone_to_profiles.sql`
- SettingsPanel now loads/saves a bilingual timezone selector below the morning digest toggle, defaults to the browser timezone, and keeps active push subscriptions synced to the saved timezone
- `/api/profile` now accepts and normalizes the `timezone` field

### Files touched
components/settings/SettingsPanel.tsx,
app/api/profile/route.ts,
lib/supabase/types.ts,
supabase/migrations/2026030901_add_timezone_to_profiles.sql,
tasks/todo.md,
tasks/lessons.md,
scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed after the admin triage cleanup
- `pnpm typecheck` passed after the admin triage cleanup
- `pnpm lint` passed after the timezone settings patch
- `pnpm typecheck` passed after the timezone settings patch
- Final `pnpm build` passed
- Final `pnpm test` passed with 28 passed and 3 skipped

### Patterns confirmed
- If the digest scheduler reads `push_subscriptions.timezone`, a profile-only timezone setting is incomplete until the active subscription row is updated too
- Browser-detected timezone defaults should be surfaced as an explicit selectable option when they fall outside the curated Gulf-heavy timezone list

## Sprint: Remote migration apply + billing renewal date + portal link (2026-03-09)

### What changed
- OPS-A: 2026030901 (timezone column) applied to remote via canonical repair sequence
- Billing page: renewal date shown for active subscriptions
- Billing page: "Access until" shown for cancelled/past_due
- Billing page: Manage billing portal link when ls_customer_portal_url is set

### Files touched
app/(dashboard)/billing/page.tsx

## Sprint: Admin shell theme switcher + bilingual nav (2026-03-09)

### What changed
- `components/admin/AdminShell.tsx`: added an optional `breadcrumb` slot, removed the inline Moon/Sun toggle, and mounted `AdminThemeSwitcher` in the header controls without changing the logout, language toggle, nav, or layout structure
- `components/admin/AdminNav.tsx`: nav items now carry bilingual labels from `useLang()`, and the admin-access info card is localized in EN/AR
- `components/admin/AdminThemeSwitcher.tsx`: restored the missing helper as a self-contained three-mode light/dark/system pill toggle that keeps the existing `fazumi_theme` storage contract intact for the rest of the app
- `specs/admin-shell-nav-theme-breadcrumb-2026-03-09.md`: recorded the slice scope, non-goals, and acceptance criteria

### Files touched
components/admin/AdminShell.tsx, components/admin/AdminNav.tsx,
components/admin/AdminThemeSwitcher.tsx, specs/admin-shell-nav-theme-breadcrumb-2026-03-09.md,
tasks/todo.md

### Verification
- `pnpm lint` passed
- `pnpm typecheck` passed

### Patterns confirmed
- When a requested shared helper is missing from the live workspace, restore the smallest self-contained version that preserves existing storage contracts instead of broadening the refactor into unrelated providers or route code
- Admin shell localization can stay page-agnostic by accepting a `React.ReactNode` breadcrumb slot while leaving page-level breadcrumb composition to the caller

## Sprint: Admin overview KPI strip refresh (2026-03-09)

### What changed
- `components/admin/AdminKpiCard.tsx`: added a reusable client KPI card with optional delta badge, variant icon circle styles, and an inline SVG sparkline that safely renders nothing when `sparkData` is empty
- `components/admin/AdminOverviewContent.tsx`: replaced the old range-based KPI row with four cards for MRR, active users, summaries today, and churn rate using the existing `AdminOverviewMetrics` shape
- `specs/admin-overview-kpi-strip-2026-03-09.md`: recorded the live workspace/type drift so the slice stays scoped to UI only

### Files touched
components/admin/AdminKpiCard.tsx, components/admin/AdminOverviewContent.tsx,
specs/admin-overview-kpi-strip-2026-03-09.md, tasks/todo.md

### Verification
- `pnpm lint` passed
- `pnpm typecheck` passed
- `pnpm test` did not complete in this shell: a CI-mode rerun showed unrelated Playwright failures after the local app server dropped and requests started failing with `ECONNREFUSED ::1:3000`
- `pwsh ./scripts/smoke.ps1` also timed out in this environment before it returned a stable suite result

### Patterns confirmed
- If a requested admin UI slice is described against an older snapshot, anchor the implementation to the live metrics type and record the drift before editing
- Inline SVG sparklines are enough for compact KPI cards when the chart is decorative and empty-state-safe behavior matters more than adding another chart abstraction

## Sprint: Emotional design audit baseline (2026-03-09)

### What changed
- `tasks/emotional-design-impl.md`: added the required Phase 0 log, baseline findings, blockers, and the scoped Phase 1-8 plan
- `specs/emotional-design-improvements-2026-03-09.md`: reused the existing emotional-design spec as the Phase 0 source of truth for the rollout
- `tasks/todo.md`: aligned Phase 0 planning with the existing EDI1-EDI8 checklist instead of creating a duplicate story section

### Files touched
tasks/emotional-design-impl.md, tasks/todo.md, scripts/ralph/progress.txt

### Verification
- Phase 0 was planning/documentation only; no product code changed yet
- Required baseline files were inspected before implementation work starts

### Patterns confirmed
- The current summary payload plus `deriveActionCenter()` already provide enough structured signal to build a calm result StatusLine without changing the AI schema first
- Existing PMF and push/digest infrastructure can be extended, but weekly-progress and inactivity notifications need their own cadence guard instead of reusing the daily digest timestamp

## Sprint: Emotional design Phase 1 alignment (2026-03-10)

### What changed
- `components/summary/StatusLine.tsx`: added a dedicated calm status line with EN/AR action/date/payment phrasing, muted icon treatment, and RTL-safe count emphasis
- `components/SummaryDisplay.tsx`: inserted the new StatusLine directly after the result header, added the `Saved ~X min` / `وفَّرت ~X دقيقة` header label, and kept the existing privacy footer untouched
- `app/globals.css`: added a 300ms opacity-only summary fade-in utility because no existing fade-in class was present
- `tasks/emotional-design-impl.md`: rewrote the log into the requested Phase 1 structure and recorded the checkpoint outcome

### Files touched
components/summary/StatusLine.tsx, components/SummaryDisplay.tsx,
app/globals.css, tasks/emotional-design-impl.md, scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed
- `pnpm typecheck` passed
- `pwsh ./scripts/smoke.ps1` was attempted for browser verification but timed out before it returned a stable result in this shell

### Patterns confirmed
- When a result-screen status treatment exists but does not match the required emotional contract, replace it with one dedicated component instead of layering more chip logic into the display file

## Sprint: Emotional design Phase 2 alignment (2026-03-10)

### What changed
- `components/history/HistoryList.tsx`: moved the first-use history empty state into the component itself, added the requested bilingual `EmptyState` copy, and kept the search/filter shell unchanged for non-empty history states
- `app/(dashboard)/dashboard/page.tsx`: added the lightweight zero-summary note directly below `DashboardBanner` with a link to `/summarize`
- `app/(dashboard)/summarize/page.tsx` and `components/SummaryDisplay.tsx`: replaced the earlier ad hoc milestone/discovery treatment with dismissible inline notices under `StatusLine`, using thresholds `1`, `5`, `10`, `25`, `50` plus the one-time 5th-summary calendar nudge and browser-local once-only gating

### Files touched
components/history/HistoryList.tsx, app/(dashboard)/dashboard/page.tsx,
app/(dashboard)/summarize/page.tsx, components/SummaryDisplay.tsx,
tasks/emotional-design-impl.md, tasks/todo.md, tasks/lessons.md,
scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed
- `pnpm typecheck` passed
- `pnpm build` passed
- `pwsh ./scripts/smoke.ps1` was attempted for browser-level verification but timed out before it returned a stable result in this shell

### Patterns confirmed
- When a phase requires one-time inline notices, drive them from the saved-result count in the page, gate them with browser-local keys, and render them directly under the existing status line instead of adding another card section later in the result

## Sprint: Emotional design Phase 8 final audit closure (2026-03-10)

### What changed
- `components/SummaryDisplay.tsx`: restored the locked six-section result/export contract by merging structured `contacts` into the links section and expanded the 5th-summary discovery copy to teach both calendar export and family sharing
- `components/history/HistoryList.tsx`: aligned the bulk history action label to `Delete all` / `حذف الكل` without changing the delete-all behavior or selector
- `app/api/dev/create-test-accounts/route.ts` and `e2e/support.ts`: collapsed repeated auth-user scans into a single seeded-account pass and cached seeded IDs so Phase 8 Playwright smoke no longer stalls before UI assertions
- `tasks/emotional-design-impl.md`, `tasks/lessons.md`, and `docs/decisions.md`: recorded the final audit evidence, the six-section output rule, and the regression-prevention lesson

### Files touched
components/SummaryDisplay.tsx, components/history/HistoryList.tsx,
app/api/dev/create-test-accounts/route.ts, e2e/support.ts,
tasks/emotional-design-impl.md, tasks/lessons.md,
docs/decisions.md, scripts/ralph/progress.txt

### Verification
- `pnpm lint` passed
- `pnpm typecheck` passed
- `pnpm build` passed
- `pnpm test` passed with `31 passed`, `3 skipped`

### Patterns confirmed
- If summary metadata grows, keep the locked parent-facing section contract stable and fold extra structured fields into an existing allowed section unless the product spec changes
- If browser smoke depends on deterministic seed accounts, scan Supabase auth once and cache seeded account IDs instead of paging through the full auth list on every helper call

## 2026-03-10 — Fix failing limits test + route.ts deduplication
- Fixed DAILY_CAP limit banner incorrectly showing /pricing link for trial users
- Removed duplicate saveSummary from route.ts; now uses shared saveSummaryRecord
- pnpm test: 31 passed, 3 skipped, 0 failed

## 2026-03-10 — Summarize conversational redesign
- Narrowed `/summarize` to a `max-w-2xl` shell, tightened the top paste card spacing, and switched summary scrolling to `behavior: "instant"` so the result snaps into view.
- Removed the Memory + Autopilot card block from the summarize flow and replaced it with a lightweight saved-memory Sparkles notice above the form.
- Added `components/summary/FollowUpPanel.tsx`, mounted it below `SummaryDisplay`, and derived 3-4 expandable follow-up questions from the existing `SummaryResult` without any new API call.
- Files touched: `specs/summarize-conversational-redesign-2026-03-10.md`, `app/(dashboard)/summarize/page.tsx`, `components/summary/FollowUpPanel.tsx`, `tasks/todo.md`, `tasks/lessons.md`, `scripts/ralph/progress.txt`
- Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` pass; test summary = `31 passed, 3 skipped, 0 failed`.

## 2026-03-10 — Next.js security branch alignment
- Confirmed the failing Vercel deploy was building `feat/incremental-zip-summarize` at `a9b5873`, which still pinned vulnerable `next@15.4.0`, while `main` already contains commit `e607a9a` upgrading Next.js to `16.1.6`.
- Aligned the local toolchain by updating `eslint-config-next` to `16.1.6`, regenerating the lockfile, and migrating `eslint.config.mjs` from `FlatCompat` to Next 16's native flat-config exports.
- Added narrow lint overrides for the newly introduced `react-hooks/purity` and `react-hooks/set-state-in-effect` rules so the security/tooling upgrade would not force unrelated refactors across pre-existing components.
- Files touched: `specs/nextjs-security-branch-alignment-2026-03-10.md`, `package.json`, `pnpm-lock.yaml`, `eslint.config.mjs`, `tasks/todo.md`, `tasks/lessons.md`, `scripts/ralph/progress.txt`
- Verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass; test summary = `31 passed, 3 skipped, 0 failed`.

## 2026-03-10 — Profile identity rollback hardening + avatar removal + preview deploy
- Fixed the two audit findings in the profile identity slice: `/settings` now shows an explicit `Remove photo` action when an avatar exists, and the avatar API now uses auth-first sync with rollback-safe profile writes plus a dedicated delete path.
- Hardened avatar storage writes to use unique object keys, clean up uploaded files on failed auth/profile updates, and prune stale avatar files only after the new identity state is safely stored.
- Extended the dashboard greeting fallback so clearing a saved name falls back to auth metadata/email immediately instead of leaving the banner blank.
- Files touched: `app/api/profile/avatar/route.ts`, `app/api/profile/route.ts`, `app/(dashboard)/dashboard/page.tsx`, `components/dashboard/DashboardBanner.tsx`, `components/settings/SettingsPanel.tsx`, `docs/decisions.md`, `tasks/lessons.md`, `scripts/ralph/progress.txt`
- Verification: `pnpm lint`, `pnpm typecheck`, `pnpm build`, and `pnpm test` pass; test summary = `32 passed, 3 skipped, 0 failed`.
- Preview deploy: `https://fazumi-whatsapp-summarizer-1i0i4la5q-founder-fazumis-projects.vercel.app`
- Patterns confirmed: mirrored profile identity fields need rollback semantics across `profiles` and Auth metadata, and replacing an input-based optional field must preserve an intentional clear/remove action in the UI.

## 2026-03-11 — Dashboard mobile dock viewport fix
- Removed the dashboard route-wrapper `translateY` animation so mobile `position: fixed` shell chrome stays anchored to the viewport instead of the page wrapper.
- Kept the route-entry polish as an opacity-only fade and documented the fixed-position regression rule in the project logs.
- Files touched: `specs/dashboard-fixed-bottom-dock-regression-2026-03-11.md`, `app/(dashboard)/template.tsx`, `tasks/todo.md`, `docs/decisions.md`, `tasks/lessons.md`, `scripts/ralph/progress.txt`
- Verification: `pnpm lint`, `pnpm typecheck`, and `pnpm test` passed; test summary = `32 passed, 3 skipped, 0 failed`.
- Patterns confirmed: do not apply wrapper transforms to dashboard subtrees that contain fixed navigation, prompts, or other persistent app chrome.
