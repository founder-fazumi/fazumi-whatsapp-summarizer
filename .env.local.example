# =============================================
# Fazumi — Web App Environment Variables
# Copy to .env.local for local development.
# =============================================

# OpenAI API key (required for summarization)
OPENAI_API_KEY=sk-your-key-here

# OpenAI model (optional, default: gpt-4o)
# OPENAI_MODEL=gpt-4o-mini

# App URL (used for metadata + redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase (get from Supabase → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only; never expose to browser
SUPABASE_SERVICE_ROLE_KEY=

# Lemon Squeezy (get from LS → Settings → API)
LEMONSQUEEZY_API_KEY=your-ls-api-key              # server-only
LEMONSQUEEZY_WEBHOOK_SECRET=your-webhook-secret   # server-only
# Legacy (supported): LEMON_SIGNING_SECRET=
LEMONSQUEEZY_STORE_ID=your-store-id               # numeric store ID

# Lemon Squeezy variant IDs (from LS → Products → each variant)
NEXT_PUBLIC_LS_MONTHLY_VARIANT=your-monthly-variant-id
NEXT_PUBLIC_LS_ANNUAL_VARIANT=your-annual-variant-id
NEXT_PUBLIC_LS_FOUNDER_VARIANT=your-founder-variant-id
